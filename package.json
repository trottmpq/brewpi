{
    "private": true,
    "workspaces": ["reactbrew"],
    "scripts": {    
        "start": "yarn --cwd reactbrew start",
        "react-server": "yarn --cwd react-scripts start",
        "flask-server": "pipenv run flask run --host=0.0.0.0",
        "build": "yarn --cwd reactbrew build",
        "test": "yarn --cwd reactbrew test",
        "eject": "yarn --cwd reactbrew eject"
     }
}